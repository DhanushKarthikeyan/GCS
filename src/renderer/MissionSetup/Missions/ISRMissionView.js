import { ipcRenderer } from 'electron';
import React, { Component } from 'react';

import '../global.css';

export default class ISRMissionView extends Component {
  render() {
    return (
      <div>The ISRMissionView</div>
    );
  }
}
